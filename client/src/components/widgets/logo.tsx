import { BadgeCheck } from "lucide-react";

export default function Logo() {
  return (
    <h2 className="font-accent font-bold text-lg tracking-widest flex items-center gap-x-1">
      PXLM <BadgeCheck className="text-accent" />
    </h2>
  );
}
